import { AppNumberDirective } from './app-number.directive';

describe('AppNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new AppNumberDirective();
    expect(directive).toBeTruthy();
  });
});
